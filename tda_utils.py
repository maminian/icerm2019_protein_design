# Helper functions for accessing TDA tools (e.g., ripser)

from ripser import ripser

def compute_all_diagrams(path):
    """
    Computes all persistence diagrams for all pdb files in the given
    directory.
    """

def extract_coordinate_embedding(pdbfile):
    """
    Extracts the coordinate embedding of a protein: just a matrix of
    atom locations.
    """
